@model IEnumerable<BusBooking>
@{
    ViewData["Title"] = "Index";
}

<div class="entity-list">
    <h2>Bus booking List</h2>
    <div class="entity-row">
        <span class="name"></span>
        <span></span>
        <span></span>
        <span></span>
        <span></span>

        <!-- Buttons Section -->
        <div class="actions">
        </div>
    </div>
    @foreach (var busBooking in Model)
    {
        <div class="entity-row">
            <span class="name"></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <!-- Buttons Section -->
            <div class="actions">
                <a asp-area="Admin" asp-controller="AdminBusBooking" asp-action="Delete" asp-route-id="@busBooking.Id" class="delete-btn">Delete</a>
            </div>
        </div>
    }
</div>
