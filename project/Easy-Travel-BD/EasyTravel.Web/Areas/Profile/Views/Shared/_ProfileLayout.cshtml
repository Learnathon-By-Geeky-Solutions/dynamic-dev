
﻿@{
    Layout = "~/Views/Shared/_Layout.cshtml"; // Specify the base layout
    ViewData["Title"] = "My Account";
}
<section class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-4 col-lg-4">
            <div class="card shadow-sm border-0 rounded-4 bg-light">
                <div class="card-body p-4">
                    <h4 class="card-title text-center mb-4 text-primary">My Order</h4>
                    <partial name="_ProfileSidePanelBookingHistory" />
                </div>
                <div class="card-body p-4">
                    <h4 class="card-title text-center mb-4 text-primary">My Account</h4>
                    <hr class="mb-4" />
                    <partial name="_ProfileSidePanelMyAccount" />
                </div>
            </div>
        </div>
        <div class="col-md-8 col-lg-8">
            <div class="card shadow-sm border-0 rounded-4 bg-light">
                @RenderBody()
            </div>
        </div>
    </div>
</section>
@section Scripts {
    <script src="~/js/profile.js" asp-append-version="true"></script>
}
@{
    IgnoreSection("Scripts"); // Ignore the section if not defined
}