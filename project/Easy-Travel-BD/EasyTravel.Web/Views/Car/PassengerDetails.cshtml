@model BookingModel

@{
    ViewData["Title"] = "Car Booking";

}

<div class="container">
    <div class="row">
        <div class="col-md-8">
            <form method="post" id="bookingForm">
                <div class="card mb-4">
                    <div class="card-header">
                        <div class="d-flex align-items-center">
                            <i class="fas fa-car text-success me-2"></i>
                            <div>
                                <h5 class="mb-0">Car Details</h5>
                                <small class="text-muted">@Model.CarBooking?.Car?.OperatorName - @Model.CarBooking?.Car?.CarType</small>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <small class="text-muted">From</small>
                                <h6>@Model.CarBooking?.Car?.From</h6>
                            </div>
                            <div class="col-md-6">
                                <small class="text-muted">To</small>
                                <h6>@Model.CarBooking?.Car?.To</h6>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <small class="text-muted">Departure</small>
                                <h6>@Model.CarBooking?.Car?.DepartureTime.ToString("dd MMM yyyy hh:mm tt")</h6>
                            </div>
                            <div class="col-md-6">
                                <small class="text-muted">Arrival</small>
                                <h6>@Model.CarBooking?.Car?.ArrivalTime.ToString("dd MMM yyyy hh:mm tt")</h6>
                            </div>
                        </div>
                    </div>
                </div>

                ```
                <div class="card mb-4">
                    <div class="card-header">
                        <div class="d-flex align-items-center">
                            <i class="fas fa-user text-warning me-2"></i>
                            <h5 class="mb-0">Passenger Details</h5>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-3">
                                <label asp-for="@Model.CarBooking!.PassengerName">
                                    Passenger Name
                                    <input asp-for="@Model.CarBooking!.PassengerName" class="form-control" />
                                </label>

                                <span asp-validation-for="@Model.CarBooking!.PassengerName" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="@Model.CarBooking!.Email">
                                    Email

                                    <input asp-for="@Model.CarBooking!.Email" class="form-control" />
                                </label>

                                <span asp-validation-for="@Model.CarBooking!.Email" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="@Model.CarBooking!.PhoneNumber">
                                    Phone Number
                                    <input asp-for="@Model.CarBooking!.PhoneNumber" class="form-control" />

                                </label>

                                <span asp-validation-for="@Model.CarBooking!.PhoneNumber" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>
                <input asp-for="Id" hidden />
                <input asp-for="TotalAmount" hidden />
                <input type="hidden" asp-for="@Model.Booking!.TotalAmount" id="totalAmount" />
                <input type="hidden" asp-for="@Model.CarBooking!.CarId" />
                <button type="submit" onclick="formSubmit('/Booking/Car')" class="btn btn-success w-100">
                    Confirm Booking
                </button>

                <button type="submit" onclick="formSubmit('/Payment/Pay')" class="btn btn-theme w-100">
                    Continue To Pay
                </button>

            </form>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Booking Summary</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <small class="text-muted">Car Type</small>
                        <h6>@Model.CarBooking?.Car?.CarType</h6>
                    </div>
                    <div class="mb-3">
                        <small class="text-muted">Journey</small>
                        <h6>@Model.CarBooking?.Car?.From to @Model.CarBooking?.Car?.To</h6>
                    </div>
                    <div class="total-section">
                        <small class="text-muted">Total Amount</small>
                        <h4 class="text-success">৳@Model.CarBooking?.Car?.Price</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>

    ```

</div>

@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    <script>
        function formSubmit(actionUrl) {
            const form = document.getElementById('bookingForm');
            form.action = actionUrl;
            form.submit(); // Now submit the form manually
        }
    </script>
}
